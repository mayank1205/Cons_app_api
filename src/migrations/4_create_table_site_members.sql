create table site_members(id serial primary key, site_id integer references sites(id), member_id integer references users(id), role varchar(20), created_at timestamp default current_timestamp, created_by varchar(255), updated_at timestamp default current_timestamp, updated_by varchar(255))